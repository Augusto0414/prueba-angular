{
    "buildCommand": "npm run build",
    "outputDirectory": "dist/CategoryApp",
    "framework": "angular",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}